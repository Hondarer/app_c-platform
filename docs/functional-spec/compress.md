# 圧縮 (compress) 機能仕様

## 概要

圧縮は、メモリ上のバイト列を圧縮し、元のバイト列へ展開する機能です。  
生成した圧縮データは、Linux と Windows の間で相互に利用できます。

## 解決する課題

プラットフォームごとに利用できる圧縮機能と既定の出力形式が異なります。  
形式を固定しなければ、一方のプラットフォームで圧縮したデータを、他方で展開できません。

raw DEFLATE は展開後の長さを含まないため、利用側が元の長さを別の情報として管理すると、データとの対応を取り違える可能性があります。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-COMPRESS-FUNC-001` <!-- cplat-req: uuid=ca5817e8-d447-41f2-9128-dc49eae13221 --> | cplat の圧縮機能は、メモリ上のバイト列を圧縮し、圧縮前のバイト列へ展開します。 |
| `CPLAT-COMPRESS-COMP-001` <!-- cplat-req: uuid=783ca66c-be47-474d-9d5a-161248f213b2 --> | cplat の圧縮機能は、Linux で生成した圧縮データを Windows で展開し、Windows で生成した圧縮データを Linux で展開できる形式を使用します。 |
| `CPLAT-COMPRESS-COMP-002` <!-- cplat-req: uuid=8f328843-a259-48d7-afb8-18445d0ee730 --> | cplat の圧縮機能は、展開後の長さと raw DEFLATE データを一つの圧縮データとして受け渡します。 |
| `CPLAT-COMPRESS-FUNC-002` <!-- cplat-req: uuid=7bb7c876-6877-4159-afd3-51955f4d7d45 --> | cplat の圧縮機能は、出力先の容量が不足する場合、出力先の範囲外へ書き込まずに容量不足を通知します。 |
| `CPLAT-COMPRESS-FUNC-003` <!-- cplat-req: uuid=a6d4c8e1-3b27-4f9a-8e05-1c7b9d2f4a60 --> | cplat の圧縮機能は、展開後の長さが 4 GiB 以上の場合、出力先へ書き込まずに上限超過を通知します。 |

## 圧縮データの形式

圧縮データは、次の形式です。

```text
[展開後の長さ: 64 ビット符号なし整数、ネットワーク バイト オーダー] [raw DEFLATE データ]
```

圧縮本体は RFC 1951 の raw DEFLATE とします。  
展開後の長さを先頭へ含めるため、利用側は長さを別に管理する必要がありません。

展開時は、圧縮データに含まれる長さと出力先の容量を比較します。  
出力先が不足する場合は展開を完了せず、容量不足として通知します。

展開後の長さが 4 GiB 以上の場合は、圧縮も展開も行わず、上限超過として通知します。

## 適用範囲外

- 入力または出力を分割して処理するストリーミング圧縮
- 圧縮レベルの選択
- gzip または zip のコンテナー形式
- ファイルを直接入出力する圧縮

## 前提と制約

展開後の長さの欄は 64 ビットです。  
扱える展開後の長さは 4 GiB 未満です。  
先頭が 32 ビットの長さ欄だった圧縮データは対象外です。

出力先の領域は利用側が用意します。

本機能の圧縮データは、先頭に cplat 固有の長さ情報を持ちます。  
raw DEFLATE だけを受け取る機能へ、圧縮データ全体をそのまま渡すことはできません。

## 関連する機能仕様と外部仕様

- [暗号 (crypto) 機能仕様](crypto.md)
- [RFC 1951 DEFLATE Compressed Data Format Specification](https://www.rfc-editor.org/rfc/rfc1951)
