# コマンド ライン引数解析 (argparser) 機能仕様

## 概要

コマンド ライン引数解析は、コマンド ラインの文字列を、あらかじめ定義した項目へ振り分ける機能です。  
フラグ、整数、文字列、位置引数を扱い、定義の誤りと利用者が与えた引数の誤りを区別します。

## 解決する課題

コマンド ライン引数を個別に解析すると、名前の照合、値の変換、必須項目の検査、誤りの説明、使い方の生成をコマンドごとに繰り返すことになります。  
定義と使い方を別々に管理すると、変更時に内容が食い違います。

また、プログラムが項目を誤って定義した場合と、利用者が誤った引数を与えた場合では、通知する相手と時点が異なります。  
本機能は、定義、解析、結果の参照を分け、二つの誤りを別の結果として扱います。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-ARGPARSER-FUNC-001` <!-- cplat-req: uuid=97da23eb-22e5-4fd9-a178-fc27df187aa5 --> | cplat のコマンド ライン引数解析機能は、フラグ、整数、文字列を型ごとの項目として定義し、対応する値を格納します。 |
| `CPLAT-ARGPARSER-FUNC-002` <!-- cplat-req: uuid=bc3ecd15-827d-4901-b684-3df6fffeb8bb --> | cplat のコマンド ライン引数解析機能は、一つのオプションに短い名前と長い名前を定義し、どちらの名前も同じ項目として解析します。 |
| `CPLAT-ARGPARSER-FUNC-003` <!-- cplat-req: uuid=7c54ba83-72a7-44ac-9662-eee2303366a8 --> | cplat のコマンド ライン引数解析機能は、複数回の指定を許可したオプションの値を、出現順に格納します。 |
| `CPLAT-ARGPARSER-FUNC-004` <!-- cplat-req: uuid=01932cde-ecd6-4cba-b936-ac35df61acd4 --> | cplat のコマンド ライン引数解析機能は、位置引数を定義順に割り当て、末尾の位置引数を可変個数として扱えるようにします。 |
| `CPLAT-ARGPARSER-FUNC-005` <!-- cplat-req: uuid=ef23b77b-48e8-4c5b-8bbf-4150c6a51d2d --> | cplat のコマンド ライン引数解析機能は、必須項目が指定されていない場合に解析を成功させません。 |
| `CPLAT-ARGPARSER-FUNC-006` <!-- cplat-req: uuid=45735ff0-edd4-46f8-a3dc-fdf3ef0eb313 --> | cplat のコマンド ライン引数解析機能は、利用者が与えた引数の誤りについて、誤りの種類、誤りがある項目、引数内の位置、説明を取得できるようにします。 |
| `CPLAT-ARGPARSER-FUNC-007` <!-- cplat-req: uuid=dc2c8b7b-d287-4c14-801a-28bb4e59a96b --> | cplat のコマンド ライン引数解析機能は、項目定義の重複または矛盾を解析前に検出し、利用者が与えた引数の誤りと区別します。 |
| `CPLAT-ARGPARSER-FUNC-008` <!-- cplat-req: uuid=522d648d-64f2-4244-8f87-ec341511d8b3 --> | cplat のコマンド ライン引数解析機能は、登録した項目定義から使い方の説明を生成します。 |
| `CPLAT-ARGPARSER-FUNC-009` <!-- cplat-req: uuid=1f06cfeb-f3f0-4fef-8638-942a1fe669d7 --> | cplat のコマンド ライン引数解析機能は、一つのプロセスで共通して使用できる一つの解析状態を提供します。 |
| `CPLAT-ARGPARSER-FUNC-010` <!-- cplat-req: uuid=9d205c94-a7c6-48b5-9c49-7241c641cd59 --> | cplat のコマンド ライン引数解析機能は、複数の解析状態を互いに独立して保持できるようにします。 |

## 対応する構文

| 構文 | 受け付ける形式 |
|---|---|
| フラグ | `-v`、`--verbose` |
| 値付きオプション | `-o value`、`--option value`、`--option=value` |
| 複数回指定できるオプション | 値付きオプションの繰り返し |
| 位置引数 | 定義順に割り当て |
| 可変長位置引数 | 位置引数列の末尾で複数の値を割り当て |
| 負の位置整数 | 次の位置引数が整数の場合に位置引数として割り当て |

短いオプションを一つの引数へ連結する形式、値を一度に複数指定する形式、`--` 以降を無条件で位置引数にする形式は受け付けません。

## 定義と解析の流れ

利用側は、解析前に項目の名前、種類、必須かどうか、複数回指定できるか、説明、結果の格納先を定義します。  
解析結果は、定義時に指定した格納先へ書き込みます。

指定されなかった項目の格納先は変更しません。  
利用側は解析前に格納先へ値を設定することで、既定値を定義できます。

一つの解析状態を使用する場合は、プロセスの終了時まで状態を保持できます。  
複数の独立した解析を同時に扱う場合は、それぞれの解析状態を明示的に保持します。

## 誤りの通知

項目名の重複、位置引数の順序矛盾など、定義側の誤りは解析開始前に検出します。  
複数の定義側の誤りがある場合は、件数と内容をまとめて取得できます。

利用者が与えた引数については、未知のオプション、値の欠落、予期しない値、整数変換の失敗、範囲超過、必須項目の欠落、単数項目の重複、個数超過を区別します。  
誤りの対象と引数列上の位置を、説明とともに取得できます。

## 適用範囲外

- サブコマンドごとに異なる項目集合を持つ構造
- 設定ファイルまたは環境変数との統合
- 複数の項目間の依存関係の検証
- シェル補完スクリプトの生成

## 前提と制約

項目は解析前に定義する必要があります。

結果の格納先は、解析と結果の利用が終わるまで有効である必要があります。

プロセスで共通する解析状態を複数のスレッドから同時に変更する場合は、利用側が呼び出しを直列化する必要があります。

## 関連する機能仕様と外部仕様

- [基盤 (base) 機能仕様](base.md)
- [コンソール (console) 機能仕様](console.md)
