# 時計と時刻 (clock) 機能仕様

## 概要

時計と時刻は、経過時間の測定、実時刻の取得、共通の時刻表現、時刻の計算と整形を提供します。

## 解決する課題

時刻の取得方法は、プラットフォームによって基準点、分解能、値の表現が異なります。  
実時刻は時刻同期によって前後するため、経過時間の測定に使うと差分が正しくなりません。

秒とナノ秒を利用側が直接計算すると、桁上がりと桁下がりの正規化を誤る可能性があります。  
時刻を保持する型のレイアウトもプラットフォーム間で一致しません。

本機能は、用途の異なる時計を分離し、共通の時刻表現と計算を提供します。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-CLOCK-FUNC-001` <!-- cplat-req: uuid=678481da-af40-4ba4-aa85-251f8727a4e9 --> | cplat の時計・時刻機能は、時刻同期による実時刻の補正に影響されず、前の取得値より小さくならない時計を提供します。 |
| `CPLAT-CLOCK-FUNC-002` <!-- cplat-req: uuid=f987ede8-ec5a-4f4d-8626-b9a1c39a6d23 --> | cplat の時計・時刻機能は、Unix epoch を基準とする実時刻を提供します。 |
| `CPLAT-CLOCK-COMP-001` <!-- cplat-req: uuid=95baad6f-4756-4c6f-96d0-da4de33f3de1 --> | cplat の時計・時刻機能は、対応するプラットフォーム間で同じレイアウトを持つ秒とナノ秒の時刻表現を提供します。 |
| `CPLAT-CLOCK-FUNC-003` <!-- cplat-req: uuid=988acf6f-ca03-44c3-863b-3322d67b8bf2 --> | cplat の時計・時刻機能は、時刻の正規化、加算、減算、比較、ミリ秒との変換を提供します。 |
| `CPLAT-CLOCK-FUNC-004` <!-- cplat-req: uuid=c5997e9b-7029-41fa-a575-fc2adca12cbd --> | cplat の時計・時刻機能は、現在時刻からミリ秒を加えた絶対期限を生成します。 |
| `CPLAT-CLOCK-FUNC-005` <!-- cplat-req: uuid=075d2293-6e2d-440b-b7f3-9184cfb2f760 --> | cplat の時計・時刻機能は、実時刻を UTC またはローカル時刻の ISO 8601 形式へ整形します。 |
| `CPLAT-CLOCK-COMP-002` <!-- cplat-req: uuid=f091fcce-1b32-4731-a147-8a3659fdfdc8 --> | cplat の時計・時刻機能は、共通の時刻表現と OS が要求する時刻表現を相互に変換します。 |

## 時計の種類

| 項目 | 単調増加クロック | 実時刻クロック |
|---|---|
| 基準点 | 不定 | Unix epoch |
| 時刻同期による補正 | 受けません | 受けます |
| 値の単調増加 | 保証します | 保証しません |
| 主な用途 | 経過時間とタイムアウト | 実時刻の記録と外部との照合 |

経過時間とタイムアウトには、単調増加クロックを使用します。  
ログの時刻など、外部の時刻と照合する値には実時刻クロックを使用します。

## 共通の時刻表現

共通の時刻表現は、秒とナノ秒を保持し、対応するプラットフォーム間で同じバイナリ レイアウトを持ちます。  
絶対時刻と単調増加クロックの値は同じ表現を使用しますが、異なる基準の値を相互に比較することはできません。

OS の機能との境界では、共通の表現と OS 固有の表現を明示的に変換します。

## 時刻の計算と整形

時刻の加減算は、ナノ秒の桁上がりと桁下がりを正規化した結果を返します。  
ミリ秒からの期限生成と、二つの時刻のミリ秒差を取得できます。

実時刻は、UTC への分解と、UTC またはローカル時刻の ISO 8601 形式への整形ができます。

## 適用範囲外

- 任意のタイム ゾーンを扱うデータベースと変換
- 日付の加減算、営業日などの暦計算
- 高分解能タイマーによる周期実行

## 前提と制約

単調増加クロックの基準点自体には意味がなく、同じ時計から取得した二つの値の差だけが有効です。

実時刻は時刻同期によって前後する可能性があります。  
処理の前後関係の判定には使用できません。

Windows の単調増加クロックの分解能はミリ秒です。  
これより細かい経過時間は測定できません。

共通の時刻表現のレイアウト互換性は x86-64 を対象とします。

## 関連する機能仕様と外部仕様

- [トレース (trace) 機能仕様](trace.md)
