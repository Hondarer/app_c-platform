# 同期 (sync) 機能仕様

## 概要

同期は、スレッドの生成と待ち合わせ、プロセス内の同期、プロセス横断の同期、待機時間の指定を提供します。

## 解決する課題

スレッドと同期オブジェクトは、Linux と Windows で生成、待機、破棄、失敗の意味が異なります。  
プロセス内で有効な同期と、複数プロセスで共有する同期も別の手続きになります。

本機能は、同期の有効範囲と読み書きの性質を選べる共通の機能として提供します。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-SYNC-FUNC-001` <!-- cplat-req: uuid=6081dde8-ac84-464a-9ec1-a1f1ecd113c2 --> | cplat の同期機能は、スレッドを生成し、完了の待ち合わせまたは切り離しを行います。 |
| `CPLAT-SYNC-FUNC-002` <!-- cplat-req: uuid=71f00767-5322-48ec-a854-98bd925cb289 --> | cplat の同期機能は、プロセス内で指定した初期化処理を一度だけ実行します。 |
| `CPLAT-SYNC-FUNC-003` <!-- cplat-req: uuid=1fd20f00-0ce2-4751-95e2-cef85a9de48d --> | cplat の同期機能は、単一プロセス内で一つの実行主体だけが取得できる相互排他を提供します。 |
| `CPLAT-SYNC-FUNC-004` <!-- cplat-req: uuid=26636c4f-592f-4394-9a8d-cfde0279f377 --> | cplat の同期機能は、単一プロセス内で複数の読み取りを許可し、書き込みを排他する読み書きロックを提供します。 |
| `CPLAT-SYNC-FUNC-005` <!-- cplat-req: uuid=e5d29b22-849a-4271-b925-6f0a49fda973 --> | cplat の同期機能は、条件が成立するまで相互排他を解放して待機し、通知後に再取得する条件待機を提供します。 |
| `CPLAT-SYNC-FUNC-006` <!-- cplat-req: uuid=0fe1d4a8-8e00-4eac-8379-a2345864bc5e --> | cplat の同期機能は、同じ識別子を使用する複数のプロセス間で有効な相互排他を提供します。 |
| `CPLAT-SYNC-FUNC-007` <!-- cplat-req: uuid=663f6cc8-86fe-4912-aa60-9c51bcf2c840 --> | cplat の同期機能は、同じ識別子を使用する複数のプロセス間で有効な読み書きロックを提供します。 |
| `CPLAT-SYNC-COMP-001` <!-- cplat-req: uuid=8e1e621a-93be-45cb-a5de-20e319b71abc --> | cplat の同期機能は、開いているプロセス横断ロックをバイト列として受け渡し、別のプロセスから同じロックを開けるようにします。 |
| `CPLAT-SYNC-FUNC-008` <!-- cplat-req: uuid=5c57f53f-b55d-4d5c-a5f0-53f79322f6bc --> | cplat の同期機能は、待機を伴う同期に、無期限、待機なし、ミリ秒単位の期限を指定できるようにします。 |
| `CPLAT-SYNC-FUNC-009` <!-- cplat-req: uuid=1064e222-e819-444a-acfa-de8f562aec32 --> | cplat の同期機能は、指定したミリ秒の間、現在のスレッドを待機させます。 |

## 同期の種類

プロセス内の同期は、相互排他、読み書きロック、条件待機を提供します。  
読み取りが多く書き込みが少ない共有データでは、読み書きロックによって複数の読み取りを並行できます。

プロセス横断の同期は、相互排他と読み書きロックを提供します。  
同じ識別子を指定したプロセスは、同じロックを開きます。

## 待機とタイムアウト

待機には、無期限、待機なし、ミリ秒単位の期限を指定できます。  
期限までに取得できなかった場合と、待機せずに取得できなかった場合は異なる結果として通知します。

OS による一時的な中断は、利用側から通常の同期結果として観測されないように扱います。

## プロセス横断ロックの受け渡し

開いているプロセス横断ロックは、別のプロセスへ渡せるディスクリプターに変換できます。  
受け取った側は、ディスクリプターから同じロックを開きます。

ディスクリプターの内容が破損している場合は、ロックを開かずに破損を通知します。  
利用側は、書き出し前に必要なディスクリプターの長さを取得できます。

## 適用範囲外

- スレッド プールとタスク スケジューリング
- スレッドの優先度とプロセッサ affinity
- 再入可能な相互排他
- デッドロックの検出と回復

## 前提と制約

同期オブジェクトを破棄する前に、他のスレッドまたはプロセスが使用していないことを利用側が保証する必要があります。

スレッドの切り離しは、同じスレッドに対して一度だけ行う必要があります。

ファイルを複数のプロセスから開けること自体は排他制御を意味しません。  
共有データの一貫性が必要な場合は、すべての参加プロセスが同じロックを使用する必要があります。

## 選択の指針

| 状況 | 使用する同期 |
|---|---|
| 単一プロセス内の排他 | プロセス内の相互排他 |
| 単一プロセス内で読み取りが多い共有データ | プロセス内の読み書きロック |
| 条件の成立まで待機 | 条件待機とプロセス内の相互排他 |
| 複数プロセス間の排他 | プロセス横断の相互排他 |
| 複数プロセス間で読み取りが多い共有データ | プロセス横断の読み書きロック |

## 関連する機能仕様と外部仕様

- [メモリ マップド ファイル (mmap) 機能仕様](mmap.md)
