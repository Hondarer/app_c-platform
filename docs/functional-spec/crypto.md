# 暗号 (crypto) 機能仕様

## 概要

暗号は、バイト列の認証付き暗号化と復号、パスフレーズからの鍵導出、暗号論的乱数の取得を提供します。  
暗号化したデータは、Linux と Windows の間で相互に利用できます。

## 解決する課題

暗号化だけでは、暗号文が改ざんされたことを検出できません。  
同じ鍵とノンスの組み合わせを再利用した場合や、予測可能な値を鍵やノンスに使用した場合も、安全性が失われます。

プラットフォームごとに異なる暗号機能を利用側が直接扱うと、初期化、形式、失敗時の扱いが分かれます。  
本機能は、アルゴリズムと受け渡し形式を固定し、利用側が安全性に関わる選択を誤る余地を限定します。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-CRYPTO-FUNC-001` <!-- cplat-req: uuid=836f4f05-235f-47cd-b75a-bad40d0489ad --> | cplat の暗号機能は、バイト列を認証付きで暗号化し、復号時に認証タグを検証します。 |
| `CPLAT-CRYPTO-COMP-001` <!-- cplat-req: uuid=7adbd775-15be-42d6-bb0d-694a2047ed06 --> | cplat の暗号機能は、Linux で暗号化したデータを Windows で復号し、Windows で暗号化したデータを Linux で復号できる形式を使用します。 |
| `CPLAT-CRYPTO-CONS-001` <!-- cplat-req: uuid=75744119-bebd-4880-bd73-bb33445c0fd9 --> | cplat の暗号機能は、認証付き暗号に AES-256-GCM、ハッシュに SHA-256 を使用します。 |
| `CPLAT-CRYPTO-FUNC-002` <!-- cplat-req: uuid=2a835e92-3f7f-40db-b730-138e38b3b80d --> | cplat の暗号機能は、暗号化しない追加データを認証の対象に含めます。 |
| `CPLAT-CRYPTO-FUNC-003` <!-- cplat-req: uuid=fa3162be-c6a4-422e-98cb-392ec449fb35 --> | cplat の暗号機能は、パスフレーズから暗号化に使用できる固定長の鍵を導出します。 |
| `CPLAT-CRYPTO-QUAL-001` <!-- cplat-req: uuid=c60d6ba2-c8ff-4970-8002-15ca4e5ab8e2 --> | cplat の暗号機能は、暗号用途に使用できる予測困難な乱数を OS の暗号論的乱数源から取得します。 |
| `CPLAT-CRYPTO-QUAL-002` <!-- cplat-req: uuid=547b171d-8cec-4d9a-bf32-efa6ee95dd90 --> | cplat の暗号機能は、認証タグが一致しない暗号文を平文として受け入れません。 |

## 暗号化の形式

暗号化の結果は、次の形式です。

```text
[暗号文: 平文と同じ長さ] [GCM 認証タグ: 16 バイト]
```

暗号化だけを行い、認証タグを省略する形式は提供しません。  
復号時は認証タグを検証し、暗号文または追加認証データが変更されている場合は復号を成功させません。

ノンスは 12 バイトで、次の形式です。

```text
[セッション識別子: 4 バイト] [連番: 4 バイト] [予約領域: 4 バイト]
```

セッション識別子と連番は利用側が指定します。  
利用側は、同じ鍵に対して同じ組み合わせを再利用しないよう管理します。

## 鍵の導出と乱数

パスフレーズから、暗号化に使用する固定長の鍵を導出できます。  
導出した鍵の強度は、元のパスフレーズの強度を超えません。

鍵、セッション識別子など、推測されてはならない値の生成に使用できる暗号論的乱数を取得できます。

## 適用範囲外

- AES-256-GCM と SHA-256 以外のアルゴリズム選択
- 公開鍵暗号と電子署名
- 鍵の保管、配送、失効
- セッション識別子と連番の採番および永続化
- TLS などの通信プロトコル

## 前提と制約

利用側は、同じ鍵に対して同じノンスを再利用してはなりません。

入力の長さは、対応するすべてのプラットフォームで同じ結果を保証できる範囲に限定します。

パスフレーズは、必要な強度を持つ値を利用側が用意します。

## 関連する機能仕様と外部仕様

- [圧縮 (compress) 機能仕様](compress.md)
- [実行時支援 (runtime) 機能仕様](runtime.md)
