# トレース (trace) 機能仕様

## 概要

トレースは、アプリケーションの診断メッセージを、OS のログ基盤、Windows の診断チャネル、ファイル、標準エラー出力へ送る機能です。  
出力先ごとに詳細度を設定し、同じメッセージを複数の出力先へ振り分けます。

## 解決する課題

運用者が参照するログ、開発者向けの詳細診断、起動直後の端末出力では、適切な出力先と詳細度が異なります。  
OS のログ基盤も Linux と Windows で異なります。

ログ ファイルは上限なく増やせず、複数プロセスから共有する場合は追記と世代更新を調停する必要があります。  
本機能は、出力先、詳細度、ファイルの世代管理、並行処理を一つのトレース状態として扱います。

出力先を設定していない利用側がログ ファイルの作成などの副作用を受けると、配置先の汚染や書き込み権限の不足を招きます。  
そのため、出力は利用側が明示的に設定した出力先だけに限ります。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-TRACE-COMP-001` <!-- cplat-req: uuid=10173424-7c4a-4aa4-9b92-c3848b4e2c34 --> | cplat のトレース機能は、Windows のイベント ログと Linux の syslog を、OS のログ基盤という同じ出力先として扱います。 |
| `CPLAT-TRACE-FUNC-001` <!-- cplat-req: uuid=b259dd63-1e3d-489b-9bee-45970b245db5 --> | cplat のトレース機能は、Windows で運用ログとは独立した ETW の診断出力を提供します。 |
| `CPLAT-TRACE-FUNC-002` <!-- cplat-req: uuid=4f8a84d6-1359-47fc-ab8d-48950c29a905 --> | cplat のトレース機能は、診断メッセージをローカル ファイルへ追記し、指定した上限に基づいて複数世代へ更新します。 |
| `CPLAT-TRACE-FUNC-003` <!-- cplat-req: uuid=5bd20322-4ed0-451e-adda-3edab6cb0c79 --> | cplat のトレース機能は、診断メッセージを標準エラー出力へ一行のテキストとして出力します。 |
| `CPLAT-TRACE-FUNC-004` <!-- cplat-req: uuid=3cb40d8e-065e-4d97-b511-e8b484c61c03 --> | cplat のトレース機能は、出力先ごとに独立した詳細度のしきい値を保持します。 |
| `CPLAT-TRACE-FUNC-005` <!-- cplat-req: uuid=ac514f56-5b76-47d3-8119-f3945c57c4fd --> | cplat のトレース機能は、出力を継続したまま、出力先ごとの詳細度を変更します。 |
| `CPLAT-TRACE-FUNC-006` <!-- cplat-req: uuid=179bd76f-73f7-4366-9d7f-a29dec2129ff --> | cplat のトレース機能は、診断メッセージへ呼び出し位置を付加できるようにします。 |
| `CPLAT-TRACE-FUNC-007` <!-- cplat-req: uuid=8e01af1a-c921-41b7-bc89-cb7761886745 --> | cplat のトレース機能は、バイナリ データを 16 進数のテキストとして出力します。 |
| `CPLAT-TRACE-QUAL-001` <!-- cplat-req: uuid=758c0235-1f8f-4491-b211-218101238cc8 --> | cplat のトレース機能は、複数のプロセスが同じローカル ファイルを共有する場合、追記と世代更新をプロセス間で調停します。 |
| `CPLAT-TRACE-QUAL-002` <!-- cplat-req: uuid=2dc58042-5f4e-4ea4-a818-65094e15bb52 --> | cplat のトレース機能は、同じトレース状態への並行呼び出しを内部で直列化する方式と、利用側が直列化する方式を選べるようにします。 |
| `CPLAT-TRACE-FUNC-008` <!-- cplat-req: uuid=e4cd1c97-f44d-4ed3-b7e1-05e7f5900c95 --> | cplat のトレース機能は、出力前のメッセージを登録済みのフックへ順に通知し、後続のフックと通常の出力を継続するか選べるようにします。 |
| `CPLAT-TRACE-FUNC-009` <!-- cplat-req: uuid=749a0ae3-77e0-4593-b89c-1c1a4a22c76d --> | cplat のトレース機能は、詳細度によらず出力する要求を受け付け、出力を無効にしていない出力先へ出力します。 |
| `CPLAT-TRACE-FUNC-010` <!-- cplat-req: uuid=1071d19b-4791-47be-a4a5-d5c78cd0fc79 --> | cplat のトレース機能は、出力先ごとのしきい値として、詳細度によらず出力する指定を受け付けず、指定が不正であることを利用側へ通知します。 |
| `CPLAT-TRACE-FUNC-011` <!-- cplat-req: uuid=94ce1fc3-c198-49fe-816e-70ec4f6212ce --> | cplat のトレース機能は、利用側が出力先の詳細度を設定するまで、すべての出力先を無効とし、ログ ファイルの作成を含む出力を行いません。 |

## 出力先と詳細度

| 出力先 | 位置付け | 既定のしきい値 |
|---|---|---|
| OS のログ基盤 | 運用者が参照する OS 標準のログ | 無効 |
| ETW | Windows の開発者向け診断 | 無効 |
| ファイル | ローカルに保持する運用ログ | 無効 |
| 標準エラー出力 | 端末での即時確認 | 無効 |

すべての出力先を既定で無効とし、利用側が出力先ごとの詳細度を設定してはじめて出力します。

詳細度は、重大度の高い順に CRITICAL、ERROR、WARNING、INFO、VERBOSE、DEBUG の 6 段階とします。  
無効を指定した出力先へは出力しません。

各出力先は、設定したしきい値以上に重大なメッセージだけを出力します。  
OS 側の詳細度が cplat より粗い場合は、複数の cplat の詳細度を同じ OS 側の詳細度へ対応させます。

### 詳細度の選び方

詳細度の段階は、INFO 以上を常時記録の対象として設計します。  
運用中のシステムで何が起きたかを、設定を変更せずに後から追えるようにするためです。

| 詳細度 | 想定する用途 | 出力量 |
|---|---|---|
| CRITICAL、ERROR、WARNING | 対処が必要な事象 | 事象の発生ごと |
| INFO | 運用上の節目となる出来事 | 常時記録しても増え続けない量 |
| VERBOSE、DEBUG | 障害の切り分けに使う詳細 | 調査のときだけ有効にする量 |

INFO には、繰り返し発生する処理の記録や、1 件ごとの詳細を含めません。  
常時記録の対象が増え続けると、保存期間が短くなり、必要な記録が先に失われるためです。

量の多い記録や、内部の状態を細かく残す記録は VERBOSE または DEBUG とします。  
これらは既定では記録せず、調査の必要が生じたときに有効にします。

### 詳細度によらない出力

詳細度による絞り込みとは別の基準で残したい記録のために、詳細度によらず出力する要求を受け付けます。  
重大度では選び切れない記録を、別の条件で選んだ結果として残すためです。

この要求は、出力を無効にしている出力先へは出力しません。  
出力しない設定を明示している出力先まで越えないためです。

この要求は、出力先ごとのしきい値としては指定できません。  
しきい値として指定すると、詳細度によらない要求だけが出力される状態になるためです。

記録に残す詳細度の表記は、詳細度による絞り込みで出力した場合と同じにします。  
詳細度によらない出力であるかどうかは選び方の違いであり、記録の重大度ではないためです。

OS のログ基盤と ETW へ渡す重大度は、常時記録の対象となる範囲へ収めます。  
cplat の外側での絞り込みによって、選んで残した記録が失われることを避けるためです。

Linux では ETW へ出力しません。  
Windows でイベント ログを使用する場合は、利用前にイベント ソースが登録されている必要があります。

## ライフサイクル

トレース状態は、生成、設定、開始、出力、停止、破棄の順で使用します。  
生成直後と停止後は、識別名、ファイル、フックを変更できます。

開始中はメッセージを出力できます。  
詳細度のしきい値だけは開始中にも変更でき、変更前後のいずれかで対象になるメッセージを取りこぼさない形で反映します。

破棄は開始中と停止中のどちらでも行え、トレース状態が所有する資源を解放します。

## 並行処理

cplat が並行呼び出しを管理する方式では、複数のスレッドが同じトレース状態を使用できます。  
利用側が管理する方式では、cplat は同じトレース状態のための同期を行いません。

異なるトレース状態は、どちらの方式でも並行して利用できます。

## メッセージの出力

通常のテキストと、バイナリを 16 進数へ変換したテキストを出力できます。  
呼び出し位置を付加する出力と、位置を付加しない出力を選べます。

利用側が時刻を指定しない場合は、出力時の現在時刻を使用します。  
指定した時刻を利用できない場合もメッセージの出力を継続し、現在時刻へ置き換えたことを通知します。

メッセージが cplat の扱える長さを超える場合は、出力先の範囲外へ書き込まず、末尾を切り詰めます。

## ファイル出力

ファイルには一行ずつ追記し、指定した長さの上限に達すると世代を更新します。  
出力先を指定しない場合は、実行ファイルの位置を基準とする既定のローカル パスを使用します。

共有モードでは、複数プロセスの追記と世代更新を調停します。  
共有モードを使用しない場合は、同じパスへ書き込むプロセスが一つであることを利用側が保証します。

## フック

登録したフックは、メッセージを出力するたびに登録順で呼び出します。  
各フックは、次のフックと通常の出力を継続するか、その時点で処理を終了するかを選べます。

## 適用範囲外

- ネットワークを介したログの収集と転送
- 構造化ログのスキーマ
- ネットワーク ファイル システム上のログ ファイル
- ログの解析と検索
- Linux での ETW 出力

## 前提と制約

Windows のイベント ログへ出力する場合は、事前にイベント ソースを登録する必要があります。

利用側が並行処理を管理する方式では、同じトレース状態への呼び出しを利用側が直列化する必要があります。

識別名、ファイル、フックは停止中にだけ変更できます。

## 関連する機能仕様と外部仕様

- [基盤 (base) 機能仕様](base.md)
- [時計と時刻 (clock) 機能仕様](clock.md)
