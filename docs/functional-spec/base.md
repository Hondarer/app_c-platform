# 基盤 (base) 機能仕様

## 概要

基盤は、cplat の各機能が共通して使用する成否、詳細エラー、実行環境の差の表現を提供します。  
利用側は、元となる標準機能や OS ごとに異なる表現を、共通の方法で判定できます。

## 解決する課題

C の標準機能と OS の機能は、失敗の通知方法と詳細値の名前空間が統一されていません。  
利用側が生の値を扱うと、機能ごとに異なる成否判定と、プラットフォームごとの分岐が必要になります。

ビルド対象の OS、アーキテクチャー、コンパイラを表す定義も処理系ごとに異なります。  
各機能が個別に判定すると、同じ環境を異なる条件で扱う可能性があります。

基盤は、これらを cplat 共通の表現へまとめます。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-BASE-FUNC-001` <!-- cplat-req: uuid=83b4ff1d-aa85-49eb-8b36-15c0a1cbb061 --> | cplat の基盤機能は、cplat の各機能が提供する操作の成功、失敗、適用の省略を共通の結果体系で通知します。 |
| `CPLAT-BASE-FUNC-002` <!-- cplat-req: uuid=b11357bc-0139-4c19-8e2d-96586a38f2d8 --> | cplat の基盤機能は、利用側の制御分岐に必要な失敗の種類を、プラットフォームに依存しない分類で通知します。 |
| `CPLAT-BASE-FUNC-003` <!-- cplat-req: uuid=9a3fc1f1-487b-4ef8-937c-cfc958f9939b --> | cplat の基盤機能は、OS 由来の詳細値を、その値の名前空間を識別できる情報とともに保持します。 |
| `CPLAT-BASE-FUNC-004` <!-- cplat-req: uuid=aa43722a-682e-4611-88e5-9b78a662d318 --> | cplat の基盤機能は、異なる OS の詳細値を、利用側の分岐に用いる共通の原因へ対応付けます。 |
| `CPLAT-BASE-FUNC-005` <!-- cplat-req: uuid=c7d6e5e7-cf9f-457b-801e-58dd7828633a --> | cplat の基盤機能は、詳細エラーを明示的に受け取らなかった場合も、現在のスレッドに記録された直前の詳細エラーを取得できるようにします。 |
| `CPLAT-BASE-FUNC-006` <!-- cplat-req: uuid=966aa949-6b3f-4410-882a-d0b9381e6308 --> | cplat の基盤機能は、共通の結果と OS 由来の詳細を、人が読める文字列へ変換します。 |
| `CPLAT-BASE-FUNC-007` <!-- cplat-req: uuid=1cd7294d-4b27-4e2a-97f9-9ad2f8141b60 --> | cplat の基盤機能は、対応する OS とアーキテクチャーを一意に識別できる共通の環境情報を提供します。 |
| `CPLAT-BASE-FUNC-008` <!-- cplat-req: uuid=d396f74a-d643-46da-89de-e398b6df5059 --> | cplat の基盤機能は、対応するコンパイラと、コンパイラごとに異なる属性を共通の環境情報として提供します。 |
| `CPLAT-BASE-COMP-001` <!-- cplat-req: uuid=c7288d31-ec79-4e43-be4e-b24a936850b5 --> | cplat の基盤機能は、共有ライブラリの公開範囲を Linux と Windows で同じ指定から定義できるようにします。 |
| `CPLAT-BASE-FUNC-009` <!-- cplat-req: uuid=573d2624-a8ac-4a11-a9e7-4502566a67cd --> | cplat の基盤機能は、共有ライブラリのロード時とアンロード時に処理を実行する共通のライフサイクルを提供します。 |

## 共通の結果

成功は一つの値で表し、失敗は成功以外の分類済みの値で表します。  
要求を受け付けたものの条件により適用しなかった結果は、成功および失敗と区別します。

分類済みの結果は、引数と状態、資源、入力解析、制御など、利用側が次の処理を判断するための粒度を持ちます。  
OS 固有の詳細値を、共通の結果として直接扱いません。

結果の値は、既存の利用側とのバイナリ互換性を維持します。  
値の配置範囲そのものには意味を持たせず、個々の分類によって判定します。

## 詳細エラーと共通の原因

OS 由来の詳細は、値の名前空間、対応する共通結果、生の値を一つの情報として保持します。  
値の名前空間を保持することで、同じ数値を持つ異なる種類のエラーを区別します。

利用側が OS 固有の値を比較しなくても済むように、対象の不存在、アクセス拒否、再試行の必要、接続拒否などを共通の原因として判定できます。

詳細を扱う機能は、明示的な返却先と現在のスレッドの直前値へ同じ内容を記録します。  
成功時と、OS 由来の詳細を持たない失敗時は、以前の詳細が誤って参照されないよう直前値をクリアします。

## エラーの文字列化

共通の結果は、分類を識別できる文字列へ変換できます。  
OS 由来の詳細は、その名前空間に対応する人が読めるメッセージへ変換できます。

文字列の生存期間やプラットフォームごとの取得方法の差は、利用側へ露出しません。

## ビルド環境の識別

OS、アーキテクチャー、コンパイラは、cplat が正規化した環境情報で識別できます。  
Windows のシステム定義は、取り込み順序によって意味が変わらない共通の条件で利用できます。

## 共有ライブラリのライフサイクル

共有ライブラリは、ロード時の初期化とアンロード時の解放を、Linux と Windows で同じライフサイクルとして扱えます。  
ロードとアンロードの処理中でも使用できる診断出力を提供します。

## 適用範囲外

- 例外による失敗通知
- OS 固有の詳細値だけを、由来を示さずに受け渡すこと
- 機能カテゴリごとに独立した結果体系
- 対応対象外の OS、アーキテクチャー、コンパイラの識別

## 前提と制約

詳細エラーの直前値はスレッドごとに独立しています。  
別のスレッドから参照することはできません。

直前値は、次に詳細エラーを扱う機能を使用すると上書きされます。  
保持が必要な場合は、利用側が取得直後に複製する必要があります。

結果と共通の原因に割り当てた既存の値は変更できません。
