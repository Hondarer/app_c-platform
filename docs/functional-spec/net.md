# ネットワーク (net) 機能仕様

## 概要

ネットワークは、IPv4 のソケット、エンドポイント、バイト オーダー変換を、Linux と Windows で共通の意味として提供します。

## 解決する課題

Linux と Windows では、ソケットの表現、初期化、終了、非ブロッキング設定、エラーの取得方法が異なります。  
システムのネットワーク定義を公開面へ露出すると、利用側もプラットフォームごとの定義と初期化順序に依存します。

送受信は指定した長さより短く完了することがあり、切断済みの接続への送信がプロセスへ影響する環境もあります。  
本機能は、これらの差を cplat の共通の意味へまとめます。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-NET-COMP-001` <!-- cplat-req: uuid=ebaf881d-b838-44c5-b667-3a573b3908d3 --> | cplat のネットワーク機能は、Linux と Windows のソケットを、値を失わない共通のハンドルとして扱います。 |
| `CPLAT-NET-FUNC-001` <!-- cplat-req: uuid=3bc0ed33-076d-4d75-9018-96544d2711fd --> | cplat のネットワーク機能は、IPv4 のアドレスとポートを共通のエンドポイントとして保持し、文字列またはホスト名から生成します。 |
| `CPLAT-NET-FUNC-002` <!-- cplat-req: uuid=9e50648e-80ba-4df0-aeb0-addab949018d --> | cplat のネットワーク機能は、16 ビット値と 32 ビット値をホスト バイト オーダーとネットワーク バイト オーダーの間で変換します。 |
| `CPLAT-NET-FUNC-003` <!-- cplat-req: uuid=aa65ffb4-1a1d-43c3-8ab9-ad63564d17e4 --> | cplat のネットワーク機能は、初回使用時に必要な初期化を行い、利用側へ初期化順序を要求しません。 |
| `CPLAT-NET-FUNC-004` <!-- cplat-req: uuid=48b58422-ea18-4087-9b62-c0c53e51850e --> | cplat のネットワーク機能は、ネットワーク操作の失敗を共通の結果と詳細エラーで通知します。 |
| `CPLAT-NET-FUNC-005` <!-- cplat-req: uuid=023aeb0b-bb14-4ca2-95b9-8782dd7a6dbe --> | cplat のネットワーク機能は、再利用、非ブロッキング、送受信タイムアウトなどの用途ごとにソケットの設定を変更します。 |
| `CPLAT-NET-FUNC-006` <!-- cplat-req: uuid=1b90cadf-047a-40b7-80b8-b43e28e0c1ff --> | cplat のネットワーク機能は、接続型ソケットで指定した長さを送り終える、または受け取り終えるまで処理を継続する送受信を提供します。 |
| `CPLAT-NET-FUNC-007` <!-- cplat-req: uuid=dc03c29b-d59c-4beb-b21b-3e98a674f587 --> | cplat のネットワーク機能は、複数のソケットから受信可能なものが現れるまで待機し、待機結果を返します。 |
| `CPLAT-NET-QUAL-001` <!-- cplat-req: uuid=1350a7d0-27c7-4caa-b98c-67b01869a8e8 --> | cplat のネットワーク機能は、切断済みの接続へ送信した場合に、呼び出し元のプロセスを終了させず、失敗として通知します。 |
| `CPLAT-NET-COMP-002` <!-- cplat-req: uuid=08451333-3dfb-49ca-a2f3-a2cff0456b27 --> | cplat のネットワーク機能は、受信方向だけを停止する操作によってソケットが閉じられた場合、利用側が保持するソケットを無効な値へ変更します。 |

## ソケットとエンドポイント

共通のソケット ハンドルは、対応するプラットフォームのソケットを可逆に保持します。  
無効なハンドルも共通の値として判定できます。

エンドポイントは IPv4 アドレスとポートを保持します。  
数値形式の IPv4 アドレスとホスト名のどちらからも生成でき、名前解決の失敗は他のソケット エラーと区別します。

## 初期化と設定

ネットワーク機能は、初回使用時にプロセスで必要な初期化を一度だけ行います。  
利用側は、個々のネットワーク操作より前に初期化を呼び分ける必要がありません。

ソケットの再利用、非ブロッキング、送信タイムアウト、受信タイムアウト、送受信バッファーなどを、用途ごとの設定として変更できます。

## 送受信と待機

指定した長さを扱う送受信は、途中までしか処理されなかった場合に残りを継続します。  
接続の終了、タイムアウト、再試行が必要な状態、その他の失敗は区別して通知します。

複数ソケットの待機は、受信可能なソケットの集合と、タイムアウトした結果を返します。

Linux で切断済みの接続へ送信した場合も、プロセスへ終了シグナルを配信させず、送信の失敗として利用側へ返します。

受信方向だけを停止する操作は、Linux ではソケットを保持し、Windows ではソケットを閉じます。  
ソケットを閉じた場合は、利用側が閉じたソケットを再利用しないよう、保持している値を無効な値へ変更します。

## 適用範囲外

- IPv6
- TLS などの暗号化通信
- DNS キャッシュ
- イベント ループと非同期 I/O の実行基盤
- メッセージ境界を持つ上位プロトコル

## 前提と制約

指定した長さを完了まで処理する送受信は、接続型ソケットを対象とします。

非ブロッキング状態またはタイムアウトを使用する場合、利用側は再試行が必要な結果と終了を区別する必要があります。

ホスト名の解決結果が複数ある場合に、利用側が使用するアドレスの選択方針までは定めません。

同じソケットに対する複数の処理を同時に行う場合は、利用側が処理を直列化する必要があります。

## 関連する機能仕様と外部仕様

- [基盤 (base) 機能仕様](base.md)
