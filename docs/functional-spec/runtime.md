# 実行時支援 (runtime) 機能仕様

## 概要

実行時支援は、プログラムが自分の実行環境を調べ、子プロセスを扱い、終了処理を管理し、実行時に機能を差し替え、機密データを保持するメモリをロックするための機能群です。

## 解決する課題

ホスト名、実行ファイル、共有モジュールの位置、プロセスの生成と権限昇格は、プラットフォームごとに取得方法と結果が異なります。  
終了処理と終了要求の捕捉も、通常終了、外部からの要求、強制終了で保証できる範囲が異なります。

実行環境ごとに機能を差し替える場合は、設定の誤りによってプログラム全体が起動できなくなることを避ける必要があります。  
機密データを保持する場合は、対象メモリが補助記憶へ書き出されないよう制御する必要があります。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-RUNTIME-FUNC-001` <!-- cplat-req: uuid=3b773c47-d43f-4504-a37c-9e538ecd2c2c --> | cplat の実行時支援機能は、現在のホストの DNS ホスト名を UTF-8 で取得します。 |
| `CPLAT-RUNTIME-FUNC-002` <!-- cplat-req: uuid=9fc54f3c-292e-4012-bce2-dd562a02d1d8 --> | cplat の実行時支援機能は、指定した関数を含むモジュールの絶対パスとファイル名を取得します。 |
| `CPLAT-RUNTIME-FUNC-003` <!-- cplat-req: uuid=00b701e2-f7c0-40e6-af40-970ad270a5d1 --> | cplat の実行時支援機能は、現在のプロセスを起動した実行ファイルの絶対パスを取得します。 |
| `CPLAT-RUNTIME-FUNC-004` <!-- cplat-req: uuid=8e487e6c-e4b7-4bea-9c88-51fed6e301fe --> | cplat の実行時支援機能は、子プロセスの起動、完了待ち、終了状態の取得、強制終了を提供します。 |
| `CPLAT-RUNTIME-FUNC-005` <!-- cplat-req: uuid=c421b2d1-0797-4b69-a531-01def85d9e0e --> | cplat の実行時支援機能は、現在の権限を確認し、Windows で権限が不足する場合は、権限昇格したプロセスを起動します。 |
| `CPLAT-RUNTIME-FUNC-006` <!-- cplat-req: uuid=8ccd9f67-6e1f-4414-b94f-3c6b71996708 --> | cplat の実行時支援機能は、権限昇格したプロセスから呼び出し元へ、処理結果とメッセージを受け渡します。 |
| `CPLAT-RUNTIME-FUNC-007` <!-- cplat-req: uuid=55b5d6d3-f35a-4518-9881-4209725ff251 --> | cplat の実行時支援機能は、通常終了時に登録済みの後処理を実行し、終了理由を通知します。 |
| `CPLAT-RUNTIME-FUNC-008` <!-- cplat-req: uuid=03412406-b0e2-4958-b534-80f80cfed11e --> | cplat の実行時支援機能は、捕捉可能な終了要求を登録済みの処理へ通知します。 |
| `CPLAT-RUNTIME-FUNC-009` <!-- cplat-req: uuid=a64f418f-74fa-4ffd-b6b3-25cb9b4d8fb4 --> | cplat の実行時支援機能は、設定に基づいて指定した機能の参照先を実行時に解決します。 |
| `CPLAT-RUNTIME-QUAL-001` <!-- cplat-req: uuid=930b8282-ef97-40d8-a465-3c250acfd820 --> | cplat の実行時支援機能は、指定された自プロセスのメモリ範囲を、OS が許可する範囲でページ アウトの対象外にします。 |

## ホストとモジュールの情報

ホスト名は、OS が保持する DNS ホスト名を UTF-8 で返します。  
完全修飾ドメイン名であることは保証しません。

指定した関数を含むモジュールの絶対パスと、そのファイル名を取得できます。  
実行ファイル本体の絶対パスも、モジュールの位置とは別に取得できます。

## プロセスと権限昇格

子プロセスを起動し、完了を待って終了状態を取得できます。  
待機を継続できない場合は子プロセスを強制終了できます。

Windows では、現在の権限が不足する場合に、自分自身を権限昇格して起動できます。  
Linux では、現在の実効ユーザーが必要な権限を持たない場合に、cplat 自身による権限昇格は行いません。

権限昇格したプロセスの結果は、一時的なファイルを介して呼び出し元へ返せます。  
この経路は、コンソールへの出力に依存せず、結果の種類とメッセージを受け渡します。

## 終了処理

登録した後処理は、通常のプロセス終了時に登録順序に従って実行します。  
cplat が終了理由を保持している場合は、その理由を後処理へ通知します。

捕捉可能な終了要求は、登録した処理へ通知します。  
強制終了、プロセスの異常終了、OS が捕捉を許可しない終了では、後処理の実行を保証しません。

## 実行時の機能差し替え

設定に、対象の機能、読み込むモジュール、解決するシンボルを指定できます。  
解決に成功した場合は、利用側がその参照先を使用できます。

設定ファイルがない場合、必須情報が不足する場合、未知の項目がある場合、型が一致しない場合、解決に失敗した場合は、その差し替えを適用しません。  
差し替えを適用できないことによって、設定を使用しない通常の起動を妨げません。

## メモリ ロック

自プロセスの指定したメモリ範囲を、OS が許可する範囲でページ アウトの対象外にできます。  
必要なページを先に物理メモリへ割り当てた上でロックすることもできます。

ロックを解除すると、対象範囲は通常のページ管理へ戻ります。

## 適用範囲外

- 完全修飾ドメイン名の解決
- 標準入出力のリダイレクトと子プロセスとの対話
- Linux での権限昇格手段の提供
- 強制終了またはクラッシュ時の後処理保証
- 差し替える機能の既定実装の提供
- 他のプロセスのメモリのロック

## 前提と制約

権限昇格したプロセスから結果を受け取る場合は、呼び出し元と昇格後のプロセスが同じ結果ファイルへアクセスできる必要があります。

実行時の機能差し替えを使用する場合、利用側は差し替えが適用されない場合の通常の処理を保持する必要があります。

差し替えた機能から同じ差し替え対象を再帰的に呼び出す構成の動作は保証しません。

メモリ ロックの上限と必要な権限は OS の設定に従います。  
要求した範囲をロックできない場合は、上限または権限の不足として通知します。

## 関連する機能仕様と外部仕様

- [コンソール (console) 機能仕様](console.md)
- [暗号 (crypto) 機能仕様](crypto.md)
