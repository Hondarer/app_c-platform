# プロンプト (prompt) 機能仕様

## 概要

プロンプトは、端末上で一行の文字列を対話的に入力する機能です。  
行編集、入力履歴、横スクロール、入力欄を保った出力、状態表示を提供します。

## 解決する課題

対話入力中に別の出力が発生すると、入力途中の行と出力が混ざり、画面上の入力欄が壊れます。  
長い入力、UTF-8 の文字境界、端末サイズの変更も、単純な一行入力では正しく扱えません。

本機能は、入力状態と画面表示をまとめて管理し、端末へ接続されていない場合は通常の一行入力へ切り替えます。

## 機能要件

| 要件 ID | cplat の要件 |
|---|---|
| `CPLAT-PROMPT-FUNC-001` <!-- cplat-req: uuid=1a822efe-e58c-4d6e-a9bb-08ef0e25011c --> | cplat のプロンプト機能は、文字の挿入、削除、カーソル移動を伴う一行入力を提供します。 |
| `CPLAT-PROMPT-FUNC-002` <!-- cplat-req: uuid=26cdc516-d0a0-404f-9497-fef505394576 --> | cplat のプロンプト機能は、呼び出し元ごとに独立した入力履歴を保持し、過去の入力を再表示します。 |
| `CPLAT-PROMPT-QUAL-001` <!-- cplat-req: uuid=07bff67e-b5fe-42a8-8c94-ff477420d059 --> | cplat のプロンプト機能は、UTF-8 のコード ポイント境界を壊さずにカーソル移動と削除を行います。 |
| `CPLAT-PROMPT-FUNC-003` <!-- cplat-req: uuid=bdf48e47-ef70-4fdd-a765-fe08b46f22ac --> | cplat のプロンプト機能は、入力が端末の表示幅を超えた場合、カーソル位置を含む範囲を横スクロールして表示します。 |
| `CPLAT-PROMPT-FUNC-004` <!-- cplat-req: uuid=7e6f08c8-a27e-4cef-93ec-ed9b0cb4cb7b --> | cplat のプロンプト機能は、入力中に別の出力を行った場合、出力後に入力欄とカーソル位置を復元します。 |
| `CPLAT-PROMPT-FUNC-005` <!-- cplat-req: uuid=26e50d6f-7a43-4116-a76a-557b59ad8909 --> | cplat のプロンプト機能は、入力欄の上部と下部へ状態を示す行を表示します。 |
| `CPLAT-PROMPT-FUNC-006` <!-- cplat-req: uuid=c2d1969f-5141-47c0-8110-197ae2eebb23 --> | cplat のプロンプト機能は、端末サイズの変更後に表示幅と状態行を新しいサイズへ合わせます。 |
| `CPLAT-PROMPT-FUNC-007` <!-- cplat-req: uuid=367bdbcc-8dbf-466b-8788-17a56ac8d18d --> | cplat のプロンプト機能は、端末へ接続されていない場合、画面制御を行わない一行入力と出力へ切り替えます。 |
| `CPLAT-PROMPT-FUNC-008` <!-- cplat-req: uuid=da01d9f2-aa34-4fbf-9598-2d6fbc962407 --> | cplat のプロンプト機能は、入力の確定、入力終端、利用者による中断を異なる結果として通知します。 |
| `CPLAT-PROMPT-FUNC-009` <!-- cplat-req: uuid=f13bd1e6-f053-40b5-8633-70cc8c16536d --> | cplat のプロンプト機能は、利用側が指定した文字列を入力欄へあらかじめ入れた状態で一行入力を開始し、利用者がその文字列を編集して確定できるようにします。 |

## 一行入力と履歴

入力中は、文字の挿入、後退削除、前方削除、行頭と行末への移動を行えます。  
カーソル位置と削除位置は UTF-8 のコード ポイント境界に限定します。

確定した入力は履歴へ追加でき、前後の履歴を呼び出して編集できます。  
履歴は呼び出し元を識別する名前ごとに分離します。

利用側は、入力欄の初期値を指定して一行入力を開始できます。  
既存の設定値を修正させる場合など、利用者に同じ内容を打ち直させないためです。  
初期値を入れた入力欄では、カーソルを末尾に置きます。  
履歴を呼び出したあとに最新の側へ戻ると、初期値、または編集途中の内容へ戻ります。

入力欄は一行のため、改行などの制御文字を含む初期値は受け付けません。  
入力欄の上限を超える初期値も、途中で切り詰めずに受け付けません。  
途中で切り詰めると、文字の途中で切れた値を編集させることになるためです。  
いずれの場合も入力を開始せず、誤りとして通知します。

## 入力欄と状態表示

入力が表示幅を超えた場合は、カーソル位置が見える範囲を表示します。  
入力内容自体は切り詰めません。

入力中の出力は、現在の入力欄を一度消去し、出力後に入力欄とカーソルを再表示します。  
標準出力と標準エラー出力のどちらへ出力するかを選べます。

状態行は入力欄の上部と下部に固定し、内容がない状態行は表示しません。  
読み取り開始時、キー入力時、入力中の出力時に端末サイズを確認し、表示を更新します。

## 端末でない場合の扱い

入力または出力が端末へ接続されていない場合は、行編集、履歴、状態行、カーソル制御を使用しません。  
入力は通常の一行読み取り、出力は通常のストリーム出力として扱います。  
入力欄の初期値は使用しません。入力側が行全体を与えるためです。  
初期値の検証は端末かどうかによらず行い、受け付けない初期値は同じく誤りとして通知します。

## 適用範囲外

- 複数行のテキスト編集
- 入力候補の補完
- 構文強調
- 端末の代替画面バッファー
- 複数の入力欄を同時に操作する画面
- 入力履歴の永続化

## 前提と制約

Windows では、プロセスとコンソールが UTF-8 を扱える状態である必要があります。

画面制御を使用する間、同じ端末への出力は本機能が提供する入力欄を保つ経路へ集約する必要があります。  
別の経路から直接出力した場合、入力欄の表示は保証しません。

端末が報告する表示幅を前提とし、文字ごとの表示幅が端末実装と異なる場合の桁位置は保証しません。

## 関連する機能仕様と外部仕様

- [コンソール (console) 機能仕様](console.md)
